"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {ArrowLeft,CarFront} from "lucide-react";import {useRouter} from "next/navigation";import {ApiError,register} from "@/lib/api/swmApi";import styles from "./register.module.css";export default function RegisterPage(){const router=useRouter();const [name,setName]=useState("");const [email,setEmail]=useState("");const [city,setCity]=useState("Quito");const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");if(name.trim().length<2){setError("Ingresa tu nombre completo.");return}if(password.length<6){setError("La contraseña debe tener al menos 6 caracteres.");return}if(password!==confirm){setError("Las contraseñas no coinciden.");return}try{setLoading(true);await register({full_name:name.trim(),email:email.trim().toLowerCase(),password,city:city.trim()||"Quito",country:"Ecuador"});router.replace("/dashboard")}catch(r){setError(r instanceof ApiError?r.detail:"No se pudo crear la cuenta.")}finally{setLoading(false)}}return <main className={styles.shell}><section className={styles.card}><header className={styles.header}><Link href="/login"><ArrowLeft size={21}/></Link><div><CarFront size={23}/><strong>SWM Care</strong></div></header><h1>Crear cuenta</h1><p>Guarda la información de tu SWM en la nube.</p><form onSubmit={submit}><label>Nombre completo<input value={name} onChange={e=>setName(e.target.value)} /></label><label>Correo<input type="email" value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Ciudad<input value={city} onChange={e=>setCity(e.target.value)} /></label><label>Contraseña<input type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label><label>Confirmar contraseña<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} /></label>{error&&<p className={styles.error}>{error}</p>}<button type="submit" disabled={loading}>{loading?"Creando cuenta...":"Crear cuenta"}</button></form><p className={styles.link}>¿Ya tienes cuenta? <Link href="/login">Ingresar</Link></p></section></main>}
